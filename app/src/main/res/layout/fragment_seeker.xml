<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimaryDark"
        android:paddingBottom="@dimen/five_pad"
        android:paddingLeft="@dimen/ten_size"
        android:paddingRight="@dimen/ten_size"
        android:paddingTop="@dimen/five_pad"
        android:weightSum="2">

        <TextView
            android:id="@+id/tv_list_tab"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/left_selected"
            android:gravity="center"
            android:padding="@dimen/dot_size"
            android:text="@string/list"
            android:textColor="@color/colorPrimaryDark" />

        <TextView
            android:id="@+id/tv_map_tab"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/right_un_selected"
            android:gravity="center"
            android:padding="@dimen/dot_size"
            android:text="@string/map"
            android:textColor="@color/colorWhite" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/seeker_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>